// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

//kyle, the cursor doesnt change when hovering over buttons and the buttons are not spaced out. As of right now, I have no solution but the feature is still cool to implement.
//possible i could anticipate the html generated by JS and add cs accordingly.
    $(document).ready(function () {
        $('#IndexGridView').DataTable({
            bFilter: false,
            "pagingType": "simple_numbers"
        });
    $('.dataTables_length').addClass('bs-select');
});
